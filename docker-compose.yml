services:
  casino:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - workspace_data:/app/workspace
    restart: unless-stopped

volumes:
  workspace_data:
